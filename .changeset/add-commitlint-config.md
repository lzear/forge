---
"@lzear/configs": minor
"@lzear/forge": minor
---

Add commitlint config. `@lzear/configs/commitlint` and `@lzear/forge/commitlint` export a Conventional Commits config (header max 100). `@lzear/configs/commitlint/emoji` and `@lzear/forge/commitlint/emoji` export a custom rule requiring commit messages to start with an emoji. `forge sync` now also syncs `lefthook.yml`.
