{
  "name": "forge",
  "version": "0.0.0",
  "private": true,
  "description": "lzear dev tooling monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -A --topological run build",
    "changeset": "changeset",
    "lint": "eslint .",
    "prepare": "lefthook install",
    "qa": "yarn build && concurrently -g 'yarn typecheck' 'yarn lint' 'yarn test' && yarn forge check",
    "release": "changeset publish",
    "setup-repo": "forge setup",
    "test": "yarn workspaces foreach -A run test",
    "typecheck": "yarn workspaces foreach -A run typecheck",
    "version-packages": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2",
    "@commitlint/cli": "^20",
    "@lzear/configs": "workspace:*",
    "@lzear/forge": "workspace:*",
    "concurrently": "^9",
    "eslint": "^9",
    "lefthook": "^2",
    "typescript": "^6"
  },
  "type": "commonjs",
  "packageManager": "yarn@4.14.1"
}
