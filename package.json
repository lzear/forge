{
  "name": "forge",
  "version": "0.0.0",
  "private": true,
  "description": "lzear dev tooling monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -A --topological run build",
    "lint": "eslint .",
    "ncu": "npx npm-check-updates --workspaces --upgrade --root --dep dev,optional,peer,prod,packageManager  && yarn && yarn up --recursive '*'",
    "prepare": "lefthook install",
    "qa": "yarn install --immutable && yarn build && concurrently -g 'yarn typecheck' 'yarn lint' 'yarn test' && yarn forge check",
    "setup-repo": "forge setup",
    "test": "yarn workspaces foreach -A run test",
    "typecheck": "yarn workspaces foreach -A run typecheck",
    "version-packages": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2",
    "@commitlint/cli": "^21",
    "@lzear/configs": "workspace:*",
    "@lzear/forge": "workspace:*",
    "@typescript/native": "npm:typescript@^7",
    "concurrently": "^10",
    "eslint": "^10",
    "lefthook": "^2",
    "npm-check-updates": "^22",
    "typescript": "npm:@typescript/typescript6@^6"
  },
  "resolutions": {
    "eslint-plugin-sonarjs/typescript": "npm:typescript@^6",
    "fflate": "0.8.2"
  },
  "type": "commonjs",
  "packageManager": "yarn@4.17.1"
}
